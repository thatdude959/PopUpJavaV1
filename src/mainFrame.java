import javax.swing.JFrame;
import javax.swing.WindowConstants;

// import jvax class package - window constants

public class mainFrame extends JFrame {
    public void init(){
        setTitle("POPUP");
        setSize(600,300 );
        setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);
        setVisible(true);
    }
    
}
